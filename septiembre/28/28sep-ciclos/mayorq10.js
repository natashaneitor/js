const arr = []
const min = 1
const max = 100

const cantidadDeElements = 10 

for(let i = 1; i<=cantidadDeElements; i++){
  arr.push(Math.floor(Math.random() * (max - min + 1) + min))
}

const mayorQue = 20

for(let i = 0; i<arr.length; i++){

}
